# Attribute diff: published provider vs generated provider

Generated by diffing the two providers' `docs/resources/*.md` on 24 August 2026.

| Resource | Published (v1.3.17) | Generated |
|---|---|---|
| `authlete_service` | 134 attributes | 224 attributes |
| `authlete_client` | 107 attributes | 118 attributes |

## 1. Breaking renames — the real migration cost

These are the **same fields under different names**. The published provider invented
abbreviated names; the generated provider uses the names from the API specification. Every
existing customer config that sets one of these breaks on upgrade.

Confirmed by reading the published provider's own source, where each schema entry carries a
comment naming the API field it maps to.

| Published name | Generated name | API field |
|---|---|---|
| `backcannel_polling_interval` | `backchannel_polling_interval` | `typo in the published provider` |
| `iss_response_suppressed` | `iss_suppressed` | `issSuppressed` |
| `token_expiration_link` | `token_expiration_linked` | `tokenExpirationLinked` |
| `encryption_alg_req_obj_match` | `request_object_encryption_alg_match_required` | `requestObjectEncryptionAlgMatchRequired` |
| `encryption_enc_alg_req_obj_match` | `request_object_encryption_enc_match_required` | `requestObjectEncryptionEncMatchRequired` |
| `front_channel_encryption_request_obj_required` | `front_channel_request_object_encryption_required` | `frontChannelRequestObjectEncryptionRequired` |
| `jwks_endpoint` | `jwks_uri` | `jwksUri` |
| `ignore_port_loopback_redirect` | `loopback_redirection_uri_variable` | `loopbackRedirectionUriVariable` |
| `supported_authorization_detail_types` | `supported_authorization_details_types` | `supportedAuthorizationDetailsTypes` |

`x-speakeasy-name-override` can preserve the published names instead, making the upgrade
non-breaking at the cost of carrying names that do not match the spec. Ory took the
abbreviated-names route and is now paying for it with a repo-wide deprecation cycle, so
matching the spec is probably right — but it must be a deliberate choice, documented in a
migration guide, not a silent break.

## 2. Why "hide what the old one hides" is the wrong instinct

The generated service resource exposes 117 attributes the published one does not. Almost none
of them are deliberate omissions — they are features the published provider never learned
about, because it has not been updated since September 2025.

| Count | Group | Recommendation |
|---|---|---|
| 18 | verifiable credentials (OID4VCI) | **Keep** — this is the coverage generation buys |
| 9 | OpenID Federation | **Keep** — this is the coverage generation buys |
| 10 | client ID metadata document | **Keep** — this is the coverage generation buys |
| 7 | client / key attestation | **Keep** — this is the coverage generation buys |
| 10 | DPoP + token exchange + JWT grant | **Keep** — this is the coverage generation buys |
| 39 | other newer settings | **Keep** — this is the coverage generation buys |
| 15 | internal / derived | **Hide** — assigned by Authlete, not customer-settable |
| 6 | key material | Special handling — see the JWK note |
| 3 | added by our overlay | Keep — required by the IdP create path |

## 3. Proposed hide list

Only these. Everything else earns its place.

- `api_key`
- `created_at`
- `descriptions`
- `handle`
- `hsks`
- `hsm_name`
- `metadata`
- `modified_at`
- `number`
- `public_key`
- `server_url`
- `service_owner_number`
- `sns`
- `sns_credentials`
- `tag`

Apply in `.speakeasy/terraform_overlay.yaml`:

```yaml
- target: $.components.schemas.service.properties.createdAt
  update:
    x-speakeasy-terraform-ignore: true
```

## 4. Fields the published provider has that we lack

Service: 27. Client: 25. Nearly all are the structured JWK block —
`kid`, `alg`, `use`, `crv`, `generate`, `key_size`, `pem_certificate`, `pem_private_key`, and
the raw JWK components `n`, `e`, `d`, `p`, `q`, `dp`, `dq`, `qi`, `x`, `y`, `k`, `x5c`.

The generated provider collapses all of that into a single `jwks` string.

**Measured against the live API on 24 August 2026.** Supplying a normally-formatted JWK Set
produced a phantom diff on every subsequent plan. Authlete returns the document with whitespace
stripped and member keys reordered (`e` ahead of `use`, `n` moved last) — semantically identical,
byte-different — and that single difference cascaded into **214 attributes** reporting as
"known after apply". Terraform's own annotation was `~ jwks = jsonencode( # whitespace changes`.

**Fixed** with a custom type comparing JWK Sets by JSON meaning rather than bytes:
`internal/provider/customtypes/jwks.go`, applied through `x-speakeasy-terraform-custom-type` in
the overlay to `client.jwks`, `service.jwks` and `service.federationJwks`. Verified end to end —
create with a pretty-printed JWK Set, then two consecutive plans, both `No changes`.

**Key generation — closed, by a different route.**

The published provider generates key material inside the resource (`generate = true`). That shape
cannot be reproduced as a Terraform function: functions must be deterministic, because Terraform
calls them during plan *and* apply, so one returning fresh random bytes would never let a plan
match its apply.

The split that does work leaves generation to `hashicorp/tls`, which already handles generating
once and holding the key in state, and adds only the deterministic transformation Authlete needs:

```terraform
resource "tls_private_key" "signing" {
  algorithm = "RSA"
  rsa_bits  = 2048
}

resource "authlete_service" "main" {
  jwks = jsonencode({
    keys = [jsondecode(provider::authlete::jwk_from_pem(
      tls_private_key.signing.private_key_pem, "signing-key-1", "RS256", "sig"))]
  })
}
```

`internal/provider/functions/jwk_from_pem.go`, registered through `additionalFunctions` in
`gen.yaml`. Handles RSA, ECDSA (P-256/384/521) and Ed25519, in PKCS#8, PKCS#1, SEC1 and PKIX
public form. Pass a private PEM for a service's signing keys or a public PEM for a client's
published keys — the output contains exactly what the input holds.

Verified against the live API: create, two consecutive clean plans, and Authlete's
`/service/jwks/get` serving the public half back, which it could only do having parsed the
supplied private key.

Not carried over: the `x5c` certificate-chain field, and validation of `alg` against Authlete's
accepted list. Neither blocks use.

### Full lists

<details><summary>117 service attributes only in the generated provider</summary>


**verifiable credentials (OID4VCI)** — `batch_credential_endpoint`, `cnonce_duration`, `credential_duration`, `credential_endpoint`, `credential_issuer`, `credential_issuer_metadata`, `credential_offer_duration`, `credential_response_encryption_alg_values_supported`, `credential_response_encryption_enc_values_supported`, `credential_response_encryption_zip_values_supported`, `credential_transaction_duration`, `credentials_supported`, `deferred_credential_endpoint`, `oid4vci_version`, `pre_authorized_grant_anonymous_access_supported`, `require_credential_request_encryption`, `require_credential_response_encryption`, `verifiable_credentials_enabled`

**OpenID Federation** — `authority_hints`, `entity_id`, `federation_configuration_duration`, `federation_enabled`, `federation_registration_endpoint`, `federation_signature_key_id`, `signed_jwks_uri`, `supported_client_registration_types`, `trust_anchors`

**client ID metadata document** — `cimd_allowlist`, `cimd_allowlist_enabled`, `cimd_always_retrieved`, `cimd_http_permitted`, `cimd_metadata_policy`, `cimd_metadata_policy_enabled`, `cimd_query_permitted`, `client_id_metadata_document_supported`, `dcr_duplicate_software_id_blocked`, `http_alias_prohibited`

**client / key attestation** — `attestation_challenge_time_window`, `client_attester_roots`, `client_attester_roots_enabled`, `client_attester_roots_only`, `key_attester_roots`, `key_attester_roots_enabled`, `key_attester_roots_only`

**DPoP + token exchange + JWT grant** — `dpop_nonce_duration`, `dpop_nonce_required`, `jwt_grant_by_identifiable_clients_only`, `jwt_grant_encrypted_jwt_rejected`, `jwt_grant_unsigned_jwt_rejected`, `token_exchange_by_confidential_clients_only`, `token_exchange_by_identifiable_clients_only`, `token_exchange_by_permitted_clients_only`, `token_exchange_encrypted_jwt_rejected`, `token_exchange_unsigned_jwt_rejected`

**other newer settings** — `access_token_for_external_attachment_embedded`, `authorization_code_duration`, `authorization_servers`, `backchannel_logout_session_supported`, `backchannel_logout_supported`, `backchannel_polling_interval`, `batch_size`, `client_assertion_aud_restricted_to_issuer`, `fapi_modes`, `front_channel_request_object_encryption_required`, `grant_management_action_required`, `grant_management_endpoint`, `id_token_aud_type`, `id_token_reissuable`, `introspection_signature_key_id`, `iss_suppressed`, `loopback_redirection_uri_variable`, `native_sso_supported`, `refresh_token_idempotent`, `request_object_audience_checked`, `request_object_encryption_alg_match_required`, `request_object_encryption_enc_match_required`, `resource_signature_key_id`, `rs_response_signed`, `supported_attachments`, `supported_authorization_details_types`, `supported_digest_algorithms`, `supported_documents_check_methods`, `supported_documents_methods`, `supported_documents_validation_methods`, `supported_documents_verification_methods`, `supported_electronic_records`, `supported_identity_documents`, `supported_prompt_values`, `supported_snses`, `token_batch_notification_endpoint`, `token_expiration_linked`, `unauthorized_on_client_config_supported`, `user_pin_length`

**internal / derived** — `api_key`, `created_at`, `descriptions`, `handle`, `hsks`, `hsm_name`, `metadata`, `modified_at`, `number`, `public_key`, `server_url`, `service_owner_number`, `sns`, `sns_credentials`, `tag`

**key material** — `credential_jwks`, `credential_jwks_uri`, `federation_jwks`, `jwks`, `jwks_uri`, `predefined_transformed_claims`

**added by our overlay** — `api_server_id`, `organization_id`, `organization_name`

</details>

<details><summary>Attributes only in the published provider</summary>


**service** — `backcannel_polling_interval`, `crv`, `d`, `dp`, `dq`, `e`, `encryption_alg_req_obj_match`, `encryption_enc_alg_req_obj_match`, `front_channel_encryption_request_obj_required`, `generate`, `ignore_port_loopback_redirect`, `iss_response_suppressed`, `jwk`, `jwks_endpoint`, `k`, `key_size`, `n`, `p`, `pem_certificate`, `pem_private_key`, `q`, `qi`, `supported_authorization_detail_types`, `token_expiration_link`, `x`, `x5c`, `y`

**client** — `alg`, `crv`, `d`, `dp`, `dq`, `e`, `generate`, `id`, `jwk`, `k`, `key_size`, `kid`, `kty`, `n`, `p`, `pem_certificate`, `pem_private_key`, `q`, `qi`, `service_api_key`, `service_api_secret`, `use`, `x`, `x5c`, `y`

</details>

## 5. Regressions that attribute diffing cannot see

Comparing attribute lists only covers what each resource exposes. Provider-level configuration
is invisible to that diff, and two regressions live there.

### On-premise and dedicated cloud could not be targeted — FIXED

The IdP host was fixed at `https://login.authlete.com`, baked in by the generator with no
override path. Because service create and delete both go through the IdP, any customer not on
Authlete's shared cloud could neither create nor delete a service.

**Not fixable through the overlay.** Declaring the IdP server as an OpenAPI server variable was
tried and does not work: Speakeasy surfaces server variables as configurable provider attributes
only for root-level servers, and resolves per-operation ones to their default at generation time.

**Fixed instead at the HTTP layer**, mirroring `authlete/authlete-typescript-sdk#36`, which solves
the same problem for the TypeScript SDK. `internal/provider/idp_routing.go` is a hand-written
`http.RoundTripper` that reroutes requests whose origin matches the IdP origin, read back from the
generated `operations.ServiceCreateIdpAPIServerList` so the match follows the spec rather than
duplicating a hostname. Three small edits in `provider.go` add the `idp_host` attribute and wrap
the transport; `persistentEdits` carries them across regenerations by three-way merge.

```hcl
provider "authlete" {
  server_url = "https://authlete.example.com"       # your cluster
  idp_host   = "authlete-login.example.com"         # your IdP
}
```

`AUTHLETE_IDP_HOST` works too. Leaving it unset returns the inner transport untouched, so the
shared-cloud path keeps exactly its generated behaviour.

The same transport also injects `apiServerId` into IdP request bodies that omit it, derived from
the cluster URL — so customers no longer look up and type a magic number. A value set explicitly
is never overwritten, and self-managed clusters that cannot be mapped still set `api_server_id` on
the resource.

Twelve unit tests in `idp_routing_test.go` cover this without a network, including that cluster
traffic and non-IdP bodies are left untouched. Verified to survive two full regenerations with no
conflict markers.

### No way to trust a private certificate authority

The published provider reads `AUTHLETE_TLS_INSECURE`. The generated one has no equivalent, which
matters for on-premise deployments behind a private CA.

**Fixed** on 24 August 2026. `gen.yaml` now sets both stubbed attributes:

```yaml
terraform:
  additionalProviderAttributes:
    httpHeaders: http_headers
    tlsSkipVerify: tls_skip_verify
```

The provider schema now carries `tls_skip_verify` and `http_headers` alongside `bearer` and
`server_url`. Note this only helps a self-managed deployment once the IdP host above is also
solvable. Prefer a trusted-CA-file option over skip-verify where possible; PingFederate and
PingDirectory expose both and warn against the insecure one.

### Deliberate, not accidental

Authlete 2.x support is dropped. The published provider carries both API generations; this one is
3.x only by decision. Worth stating in the migration guide rather than leaving customers to find
out.
